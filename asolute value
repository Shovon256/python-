def absolute_value(number):
 
    if number >= 0:
        return number
    else:
        return -number

print("The absolute value of the 89 is ", absolute_value(89))
print("The absolute value of the -160 is ", absolute_value(-160))
